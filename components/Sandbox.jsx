import React, {useState} from "react";
import { StyleSheet, View, Text } from "react-native";

const Sandbox = ({submitTodo}) => {
    return (
        <View>
            <Text>Hello</Text>
        </View>
    );
}

const styles = StyleSheet.create({
    
});

export default Sandbox;